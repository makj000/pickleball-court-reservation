 what would revive this blind booking around 8 ?

Three things would revive it — one code-free AWS change gets you back today, and one template change makes it durable. 有三种方式可以救活它——一个不改代码的 AWS 改动今天就能恢复，另一个模板改动是长期方案。

1. Stopgap (no code, works for today 7:45 AM): re-enable the old tick rule:
aws events enable-rule --name pickleball-hourly-scan
It fires every 15 min with a default aws.events event; at exactly 7:45 AM PT the handler pre-caches the JWT and queues the release probe session (SQS, 780s delay → starts ~7:58, blind-books at 8:00:00). All other ticks are no-ops (~2ms) since SCANS_ENABLED=false. Downside: 96 invocations/day and it lives outside CloudFormation.
临时方案（不改代码，今天 7:45 就生效）：重新启用旧规则 pickleball-hourly-scan。它每 15 分钟触发一次默认 aws.events 事件；7:45 整时 handler 会预缓存 JWT 并把抢订会话排入 SQS（延迟 780 秒 → 约 7:58 开始，8:00:00 盲订）。其余触发都是约 2ms 的空跑。缺点：每天 96 次调用，且游离在 CloudFormation 之外。
2. Proper fix: add a dedicated weekend 7:45 rule to iml — cron(45 14 ? * SAT,SUN *) targeting the Lambdawith no Input override (the default scheduled event carries "source": "aws.events", which is what monitor.py:73 matches), plus the matching AWS::Lambda::Permission. Deploy via scripts/he rule that got dropped in the CloudFormationmigration (commit 0dde714) — it broke after 6-28, which is why 7-18/7-19 were missed.                                              正式修复：在 infra/pickleball-monitor.yaml 加一条周末14 ? * SAT,SUN *)，目标为 Lambda 且不设 Input覆盖（默认事件自带 "source": "aws.events"，正是 monitor.py:73 匹配的字段），再加对应的 AWS::Lambda::Permission，用                 scripts/deploy-stack.sh 部署。这正是 CloudFormation   那条规则，6-28 之后失效，导致 7-18/7-19 被漏订。
3. Two hardening caveats worth fixing while you're in there:                                                                         - _queue_release_probe_session_if_needed (scheduler hour == 7 and minute == 45; if EventBridge fires aminute late, the whole morning is skipped. Loosening it to a window (e.g. 7:44–7:50, guarded by the existing                       release_probe_session_date dedupe) removes that singl
  - All these crons are UTC: 45 14 is 7:45 AM only during PDT. In November it becomes 6:45 AM PT and the hour check silently fails again (the existing prep/report rules share this DST
两个顺手要修的隐患：一是 scheduler.py:1347 要求正好 7 点 45 分，EventBridge 晚触发一分钟就整个早上跳过——建议放宽成时间窗（如 7:44–7:50，已有 release_probe_session_date 去重兜底）45 14 只在夏令时等于 7:45，11 月后会变成 6:45而静默失效（现有 prep/report 规则也有同样的夏令时漂移问题）。

My recommendation: do 2 + the minute-window loosening now (it's 12:30 AM, plenty of time before today's 7:45), and skip 1 unless thedeploy hits trouble. Want me to make those changes ane edit and deploy-stack.sh run are state-changing, soI'll show you the diff and command first — and per your preference, tell me whether to run the deploy in Claude or you'll run it in your own terminal.

我的建议：现在就做方案 2 加放宽分钟判断（现在是凌晨 1及）；方案 1仅作部署出问题时的兜底。要我改并部署吗？模板修改和 deploy-stack.sh 都会改变状态，我会先给你看 diff 和命令——另外按你的习惯，告诉我部署是在 Claude 里跑还
